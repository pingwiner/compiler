var arr = {1, 2, 3, 4, 5};

fun f1(x, y) {
    return x / y;
}

fun main(arg1, arg2) {
    a = f1(1, arg1);
    return a;
}

fun _div(x, y) {
    retVal = 0;
    {
        x = x - y;
        retVal = retVal + 1;
    } while (x >= y);
    return retVal;
}